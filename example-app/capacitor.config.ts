import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'example-app',
  webDir: 'dist/example-app/browser'
};

export default config;
